# Define a directory to load monitor definitions from. Definitions take the form
# of individual yaml files.
heartbeat.config.monitors:
  # Directory + glob pattern to search for configuration files
  path: ${path.config}/monitors.d/*.yml
  # If enabled, heartbeat will periodically check the config.monitors path for changes
  reload.enabled: false
  # How often to check for changes
  reload.period: 5s

 # Comprobar estado de servicio https://www.elastic.co/es/blog/uptime-monitoring-with-heartbeat-and-the-elastic-stack

heartbeat.monitors:
- type: icmp
  id: ping-myhost
  name: My Host Ping
  hosts: [172.18.0.5]
  #hosts: ["host.docker.internal"]
  schedule: '*/5 * * * * * *'
#- type: http
 # schedule: '@every 30s'
 # urls:
#    - https://www.backmarket.es/es-es/l/imac-qwerty-espanol/c5019c61-18f4-46f9-a052-6b989400a434#model=999%20iMac&year_date_release=79%202020

processors:
  - add_cloud_metadata: ~
  - add_docker_metadata: ~

#output.file:
  #path: "/data"
  #filename: heartbeat.json

output.logstash:
  hosts: ["logstash:5046"]



